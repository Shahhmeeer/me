# The contact form's keys (ADR-0004). Copy this file to `.env.local`, which
# git ignores, and fill it in, or run `docs/agents/keys-wizard.sh`, which
# creates the keys with you and writes them here and to Vercel. Never a
# value in this file: it is committed.
RESEND_API_KEY=
TURNSTILE_SECRET_KEY=
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
